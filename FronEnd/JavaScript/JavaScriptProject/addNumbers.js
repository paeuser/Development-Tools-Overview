// practice.js
// A simple function to add two numbers and log the result
function addNumbers(a, b) {
    const result = a + b;
    console.log('Result:', result);
    return result;
}

// Call the function with sample values
addNumbers(3, 5);
